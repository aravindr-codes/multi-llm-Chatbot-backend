# MulLLMChatapp UI

## Install dependencies
From `UI/mulLLMChatapp`:
Install dependencies with `npm install`.

```bash
npm install
```

## Start the dev server
```bash
npm start
```

Open `http://localhost:4200/`.

## Build
```bash
npm run build
```

The production build outputs to `dist/`.
